All functions in this folder assumes (unless otherwise posted) that the input 
bit length to be at least 45 bit-aligned even though some functions are less 
restrictive, i.e. only require 46, 47, etc. bit-aligned coefficients.