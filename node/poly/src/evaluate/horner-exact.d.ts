/**
 * Returns the exact result of evaluating a univariate polynomial (with
 * coefficients being double floating point expansions) using Horner's method.
 */
declare function HornerExact(p: number[][], x: number): number[];
export { HornerExact };
