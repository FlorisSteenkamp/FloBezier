/**
 * Returns the exact (bar underflow/overflow) result of evaluating a univariate
 * polynomial using Horner's method.
 * @param p
 * @param a
 */
declare function evaluateExact(p: number[][], a: number[]): number[];
export { evaluateExact };
